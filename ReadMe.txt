Please follow the below steps to run application with MongoDB.

1) Start the windows service named "MongoDB" (if it is stopped)
2) Open appsettings.json file.
3) Under "BookstoreDatabaseSettings" section replace your mongoDB server connection string for section name    "ConnectionString".
4) Save the file and run the application.